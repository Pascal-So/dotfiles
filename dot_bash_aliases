alias ga="git add"
alias gd="git diff"
alias gds="git diff --staged"
alias gc="git commit"
alias go="git checkout"
alias gs="git status"
alias gl="git lgb"

alias inode="stat -c %i"

alias fucking=sudo

function swap()
{
    local TMPFILE=tmp.$$
    mv -i "$1" $TMPFILE && mv "$2" "$1" && mv $TMPFILE $2
}

alias mv="mv -i"
alias cp="cp -i"
alias ls="ls --color=always"
alias less="less -r"
alias grep="grep --color=auto"
alias fgrep="fgrep --color=auto"
alias egrep="egrep --color=auto"

alias cdtmp='cd $(mktemp -d)'

# alias subl=subl3
alias ocli="octave-cli"
alias e=emacs
alias s=subl
alias yt2mp3='youtube-dl --extract-audio --audio-format mp3'

alias ni=ninja

alias cds="cd /mnt/kosmos/documents/eth/"
alias cdk="cd /mnt/kosmos"

alias la="ls -ahop"
alias lt="ls -ahoptr"

alias cmr="cmake -DCMAKE_BUILD_TYPE=Release"
alias cmd="cmake -DCMAKE_BUILD_TYPE=Debug"
alias cmrd="cmake -DCMAKE_BUILD_TYPE=Relwithdebinfo"

alias hx=helix
alias pn=pnpm
